
// Clears the entire screen
int clear_screen();

// Writes a single charater to screen and advances the cursor
int write_vga_char(int character);

// Sets the cursor position
int set_vga_cursor(int position);
